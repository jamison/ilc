# ILC Phase 649-654 Sequence Lock v0.1

Status: locked
Date: 2026-04-14
Phase: 649
Owner lane: G8 MVP touchpoints runtime/interface closure

## 1. Window identity and authorization basis

`window_649_654_sequence_lock_primary_gate`
`window_649_654_is_concrete_window_623_plus_runtime_lane`
`option_d_posture_active_after_649`
`no_cdl_062_opening_in_649_654`
`window_649_654_preserves_post_648_runtime_priority`

Window 649-654 is the concrete execution form of the long-carried `Window 623+`
runtime/interface lane.

Human direction 2026-04-14: the runtime/interface closure packet for the MVP
touchpoints should be drafted and executed as a concrete numbered window rather
than left as a vague roadmap label.

Window 613-619 closed rows 1-4 in spec form only and left them at
`spec_closed_runtime_pending`.

Window 642-648 preserved Window 623+ as the broader runtime/interface priority
continuation after bounded exact-numeric and security hardening.

Window 649-654 is therefore locked as a runtime/interface closure lane only,
not a new constitutional vehicle.

No new CDL or ADR opening is authorized here.

## 2. Touchpoint map and checklist target

`rows_1_through_4_target_runtime_closed_after_649_654`
`rows_5_through_9_not_closed_by_649_654`

The five MVP touchpoints close across four runtime phases:
- Phase 650: public init/admission runtime
- Phase 651: public receipt issuance and query runtime
- Phase 652: visible ECU runtime plus delayed visible ILC settlement runtime
- Phase 653: public wallet runtime integration

Checklist target in this window:
- rows 1-4 are the target checklist rows for runtime closure
- rows 5-9 remain later-lane items after this window

This window is allowed to move rows 1-4 from `spec_closed_runtime_pending` to
`runtime_closed` if and only if the runtime phases and closure gate pass.

This window may not claim rows 5-9 are closed, materially satisfied, or
implicitly resolved by runtime progress on rows 1-4.

## 3. Inherited boundary state

`wallet_boundary_576_581_unchanged_in_window_649_654`
`claimability_state_remains_deferred_in_649_654`

The following inherited boundaries remain binding in this window:
- Phase 576 and Phase 581 read-only wallet boundary
- Phase 587-589 public identity, receipt, and settlement-linked legitimacy
  boundary stack
- Phase 609 ECU / ILC / bounded-runtime separation
- Phase 612 two-form MVP gate rule
- ADR-0028 `Option D` active posture
- Window 624-630 bounded ECU active-layer runtime as inherited runtime context
- Window 631-641 exact-numeric and residual numeric cleanup as inherited machine
  contract context
- Window 642-648 bounded touched security hardening as inherited runtime hygiene
  context

`claimability_state` remains `deferred`.

No wallet write, spend, transfer, withdrawal, generalized ECU transfer, ILC
transferability, or sovereign-substrate claim is authorized in this window.

## 4. Runtime-wide numeric and security guardrails

`exact_numeric_and_non_finite_guard_applies_to_new_runtime_surfaces`
`canonical_json_compact_and_allow_nan_false_required_for_machine_surfaces`

Every new runtime/public numeric boundary touched in Window 649-654 must:
- use the exact-numeric rule
- reject non-finite numeric ingress
- emit canonical string or canonical exact-numeric machine contracts rather than
  float leakage

Every machine-verifiable or machine-consumed JSON surface touched in this window
must use:
- sorted keys
- compact separators
- `allow_nan=False`

Runtime behavior must fail closed with machine tokens rather than permissive
fallback behavior.

Epoch-based protocol state transitions remain authoritative over wall-clock
triggers.

No fix in this window may reintroduce predictable PRNG in runtime/security
paths, broad exception swallowing on touched boundaries, or bounded-stream
regressions already closed in Window 642-648.

## 5. Per-phase scope constraints

Per-phase scope is fixed as:
- Phase 649: sequence lock only
- Phase 650: bounded public init/admission runtime only
- Phase 651: bounded receipt issuance/query runtime only
- Phase 652: bounded visible ECU and delayed visible ILC lifecycle runtime plus
  read-only coupling-invariants diagnostics only
- Phase 653: bounded public wallet runtime integration only
- Phase 654: runtime coherence, checklist delta, capsule, and handoff only

Each runtime phase must remain inside its inherited spec contract and must not
be used to reopen constitutional law, broader public RC claims, or wallet
authority widening.

## 6. Routing and exclusions

After this window:
- the runtime blocker beneath rows 1-4 may be removed if the window passes
- `Option D` remains active
- no wallet widening is authorized
- no ILC transferability is authorized
- no sovereign substrate selection is authorized
- no `CDL-062` opening is authorized

The next major blocker after honest runtime closure of rows 1-4 remains the
later-lane governance/privacy/substrate route, especially the
coupling-invariants governance lock.

Window 649-654 does not by itself authorize broader public RC claims that
depend on rows 5-9.
