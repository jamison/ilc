# ILC Phase 456 Fix 3 Pre1 Recovery-Rule Family Admissibility and Prerequisite Contract v0.1

Status: Phase-456 Fix-3-Pre1 admissibility and prerequisite contract
Date: 2026-03-25
Owner lane: G8 Constitution Cluster A

## 1. Admissible carry-forward baseline

The admissible carry-forward baseline for later phases remains:

- `Subfamily A - epoch-window variants`
- `Subfamily B - clamp-floor variants`

These families remain admissible as continuity anchors even if later phases add a new structural
candidate class.

## 2. Oscillator admissibility rule

The following oscillator candidates are admissible only as future mechanism classes, not as already
approved execution targets:

- `oscillating_production_band_short_period`
- `oscillating_production_band_long_period`

Fix 4 must not freeze an oscillator family unless the executable mechanism surface exists first.

## 3. Fixed weak-field challenger policy

`mixed_queue_and_production` remains the default fixed weak-field challenger unless a later brief
removes it explicitly with justification.

Fix 5 field composition decision:

- Fix 4 must record whether strong production-band carry-forwards remain in the Fix 5 comparison field.
- Retaining strong production-band carry-forwards may make the 10 percentage-point organic-production threshold structurally unachievable.

This decision must be explicit before a later execution phase is authorized.

## 4. Implementation exit criteria

A later implementation phase must establish at minimum:

- a named executable Scenario-5 recovery-rule abstraction,
- a runnable uniform production-band surface,
- a runnable clamp-floor surface,
- an explicit decision on whether oscillator support is implemented,
- and enough parameter surface documentation for later brief freeze without hidden widening.

Fix 4 must not freeze a new execution family until those exit criteria are satisfied.

## 5. Non-authorization statement

No CDL-050 opening or ratification occurs in Phase 456 Fix 3 Pre1.

This prerequisite contract does not authorize execution of a new recovery-rule family.
